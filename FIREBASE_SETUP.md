# Налаштування Firebase для Delivery Tracker

## Крок 1: Створення Firebase проекту

1. Перейдіть на [Firebase Console](https://console.firebase.google.com/)
2. Створіть новий проект або виберіть існуючий
3. Додайте Android додаток:
   - Package name: `com.myapp` (має відповідати `applicationId` в `android/app/build.gradle`)
   - Завантажте `google-services.json` файл

## Крок 2: Заміна google-services.json

**ВАЖЛИВО:** Для повної функціональності потрібен реальний файл з Firebase Console!

1. Завантажте `google-services.json` з Firebase Console
2. **Замініть** файл `android/app/google-services.json` на реальний з Firebase
3. Переконайтеся, що package name відповідає `com.myapp`
4. **Примітка:** Зараз використовується демо-файл для збірки, але для реальних сповіщень потрібен справжній файл!

## Крок 3: Налаштування Firebase Cloud Messaging

1. У Firebase Console перейдіть до **Cloud Messaging**
2. Увімкніть Cloud Messaging API
3. Для тестування можна використовувати тестові сповіщення з консолі

## Крок 4: Перевірка налаштувань

Після встановлення `google-services.json`:
1. Перезапустіть Metro bundler: `npm start -- --reset-cache`
2. Пересоберіть додаток: `npm run android`
3. Перевірте логи - має з'явитися FCM Token

## Як працює система:

1. **Коли додаток відкритий:**
   - Firebase Messaging отримує повідомлення
   - Статус доставки оновлюється автоматично кожні 10 секунд
   - Сповіщення показуються через `react-native-push-notification`

2. **Коли додаток закритий:**
   - Заплановані сповіщення спрацьовують через системний AlarmManager
   - Background handler обробляє повідомлення від Firebase
   - Статус доставки оновлюється навіть коли додаток закритий

3. **Заплановані сповіщення:**
   - Через 1 хвилину: "📦 Замовлення прибуло до відділення"
   - Через 2 хвилини: "🎉 Доставка завершена!"

## Важливо:

- `google-services.json` має бути реальним файлом з Firebase Console
- Package name має точно відповідати в Firebase та в `build.gradle`
- Для production потрібно налаштувати сервер для відправки FCM повідомлень

